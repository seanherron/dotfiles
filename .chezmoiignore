CLAUDE.md
README.md
{{ if ne .chezmoi.os "darwin" }}
# --- macOS-only: never deploy these on Linux remote servers ----------------
# GUI editor settings (wrong path on Linux — these map under ~/Library).
Library
# GUI terminal emulators / Mac apps.
.config/ghostty
.config/cmux
# macOS-only one-shot scripts. NB: .chezmoiignore matches the *target* name,
# which for run_ scripts is the source name minus the run_onchange_ prefix.
install-berkeley-mono.sh
install-cursor-extensions.sh
install-vscode-extensions.sh
macos-defaults.sh
# pam-u2f sudo hook is macOS-specific (edits /etc/pam.d/sudo_local).
setup-pam-u2f-sudo.sh
{{ end }}
{{ if ne .profile "work" }}
# --- work-profile-only: never deploy on personal machines ------------------
# Security-key (pam-u2f) sudo hook is a work-machine policy.
setup-pam-u2f-sudo.sh
{{ end }}
