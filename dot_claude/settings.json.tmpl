{{- $disabledMcpServers := "[]" -}}
{{- if lookPath "op" -}}
{{- $disabledMcpServers = onepasswordRead (printf "op://%s/Claude Code - Disabled MCP Servers/notesPlain" .onepasswordVault) -}}
{{- end -}}
{
  "model": "opus",
  "effortLevel": "medium",
  "defaultMode": "auto",
  "disabledMcpServers": {{ $disabledMcpServers }}
}
