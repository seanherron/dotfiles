# gh CLI config. Tokens live in the OS keychain (`gh auth login`), not here.
# Docs: https://cli.github.com/manual/gh_config

git_protocol: ssh
editor: code --wait
prompt: enabled
prefer_editor_prompt: disabled
pager: delta
aliases:
    co: pr checkout
    pv: pr view --web
    rv: repo view --web
